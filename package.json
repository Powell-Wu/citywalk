{
  "name": "citywalk-for-two",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5"
  }
}
